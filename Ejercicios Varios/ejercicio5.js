var baseMenor = Number(prompt("ingrese el valor de la base menor"));
var baseMayor = Number(prompt("ingrese el valor de la base mayor"));
var altura = Number(prompt("ingrese el valor de la altura"));

areaDeTrapecio = (altura * ((baseMenor + baseMayor) / 2) );

alert(" el area del trapecio es: " + areaDeTrapecio);